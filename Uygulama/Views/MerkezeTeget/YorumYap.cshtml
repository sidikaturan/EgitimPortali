@using EgitimPortali.Request.Yorumlar
@model YorumlarPostRequest
@*
    For more information on enabling MVC for empty projects, visit http://go.microsoft.com/fwlink/?LinkID=397860
*@
<div class="leave-comment-form" id="comment">
    <h3 class="aside-title">Yorum Yaz</h3>
    <form asp-controller="MerkezeTeget" asp-action="Add" method="post" id="form-comment-add">
        <div class="form-group">
            <textarea name="Name" id="txtitle" class="form-control" placeholder="Yorum yaz..." required=""></textarea>
        </div>
        <input type="hidden" id="blogid" name="DersIcerikleriID" value="@ViewBag.id" />
        <div class="submit text-end">
            <button class="btn btn-style btn-style-3 mt-2" id="btn2">Gönder</a>
        </div>
    </form>
</div>
<script type="text/javascript">
    $("#btn2").click(function () {
        let values = {
            Name: $("#txtitle").val(),
            DersIcerikleriID: $("#blogid").val()
        };
        $.ajax({
            type: "post",
            url: "/MerkezeTeget/YorumYap/",
            data: values,
            success: function (func) {
                let result = jQuery.parseJSON(func);
            }
        });
    });
</script>
<script src="https://code.jquery.com/jquery-3.5.1.min.js" type="text/javascript"></script>