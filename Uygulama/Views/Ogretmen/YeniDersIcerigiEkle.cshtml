@using EgitimPortali.Request.DersIcerikleri
@model DersIcerikleriPostRequest
@{
    ViewData["Title"] = "YeniDersIcerigiEkle";
    Layout = "~/Views/Shared/_OgretmenLayout.cshtml";
}

<head>
    <meta charset="UTF-8">

    <script src="https://code.jquery.com/jquery-3.5.1.min.js" crossorigin="anonymous"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/3.4.1/js/bootstrap.min.js"></script>
    <link href="https://cdn.jsdelivr.net/npm/summernote@0.8.18/dist/summernote-bs4.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/summernote@0.8.18/dist/summernote-bs4.min.js"></script>
</head>
<h1>Yeni Ders İçeriği Ekle</h1>

@using (Html.BeginForm("YeniDersIcerigiEkle", "Ogretmen", FormMethod.Post))
{
    <input type="hidden" asp-for="KonularID" value="@ViewBag.id" />
    <br />
    @Html.Label("İçerik Adı")
    @Html.TextBoxFor(x=>x.Name, new { @class = "form-control" })
    <br />

    @Html.Label("Ders İçeriği")
    <textarea id="summernote" asp-for="@Model.Icerik" class="form-control" required=""></textarea>
    <br />
    @Html.Label("Ders Görseli")
    @Html.TextBoxFor(x=>x.Resim, new { @class = "form-control" })
    <br />
    @Html.Label("Ders Pdfi")
    @Html.TextBoxFor(x=>x.PdfYolu, new { @class = "form-control" })
    <br />

    <script>
        $('#summernote').summernote({
            placeholder: 'İçerik',
            tabsize: 2,
            height: 200,
            toolbar: [
                ['style', ['style']],
                ['font', ['bold', 'underline', 'clear']],
                ['color', ['color']],
                ['para', ['ul', 'ol', 'paragraph']],
                ['table', ['table']],
                ['insert', ['link', 'picture', 'video']],
                ['view', ['fullscreen', 'codeview', 'help']]
            ]
        });
    </script>

    <button class="btn btn-info">Oluştur</button>
}

